cd /c   → /c 드라이브 접속
cd ..  →상위 폴더
ls →리스트 목록
cd adfd/ →adfd 폴더 접속
mkdir sjjsjs →sjjsjs 디렉토리 생성
touch text.txt → text.txt 빈 파일 생성
git clone 깃주소 → 파일 내에 .git 생성
git config --global user.name "닉네임"  →닉네임 생성
git config --global user.email 본인@이메일   →이메일 등록
 
